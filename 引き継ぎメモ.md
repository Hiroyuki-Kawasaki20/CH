# CHかんばんセット — AI引き継ぎメモ

> **目的**: 別のAIチャットに移行しても、同じ前提・文脈で作業を即座に再開できるようにするための完全な引き継ぎドキュメント。  
> **作成日**: 2026-05-30  
> **最終更新**: 2026-06-08

---

## 1. プロジェクト概要

### 何をするツールか
CH（シャシー）ラインの **かんばん自動仕分け・工程割当・セットボード管理** を行うPythonデスクトップアプリケーション。

### 元プロジェクトとの関係
既存の「かんばん自動仕分け」プロジェクト（4工程・複数作業者対応）を、**CH固有の要件に特化してカスタマイズ**したもの。

### CH固有の最大の違い
| 項目 | 元プロジェクト | CHかんばんセット |
|------|--------------|-----------------|
| 作業者数 | 複数（4工程） | **1人**（メイン/リリーフ2工程） |
| オーバーフロー処理 | 次工程へ分散 | **リリーフ工程へ** |
| 工程割当 | 工程ルールCSVベース | **入車時間ベース自動割当** |

---

## 2. 技術スタック・環境

| 項目 | 値 |
|------|-----|
| 言語 | Python 3.x |
| conda環境 | `DIG_new` |
| Pythonパス | `C:/Users/1588386/.conda/envs/DIG_new/python.exe` |
| GUI | CustomTkinter 5.2.2 + tkinter/ttk |
| データ処理 | pandas, numpy |
| Excel出力 | openpyxl |
| セットボード | HTML/CSS/JS（ブラウザベース）+ Qt/QML版（PySide6） |
| テスト | pytest |
| OS | Windows |
| エンコーディング | UTF-8（CSV読込はCP932フォールバック対応） |

### 起動コマンド
```bash
conda activate DIG_new
cd CHかんばんセット
python -m src.app.gui
```

### テスト実行
```bash
C:/Users/1588386/.conda/envs/DIG_new/python.exe -m pytest tests/ -v
```
> **注意**: PowerShellターミナルに `git` コマンドは利用不可。ファイルベースのツールでdiff/contextを取得する。

---

## 3. ディレクトリ構成と各ファイルの役割

```
CHかんばんセット/
├── src/                          # メインソースコード
│   ├── app/
│   │   └── gui.py                # CustomTkinter GUI（≈1800行）メイン画面
│   ├── models/
│   │   └── constants.py          # 全定数定義（工数係数、休憩時間、色、混載ポリシー等）
│   ├── services/
│   │   ├── data_loader.py        # CSV/Excel読込、正規化、DataManagerクラス（≈700行）
│   │   ├── sorter.py             # 仕分けロジック:グループ化・混載（≈800行）
│   │   ├── process_assigner.py   # 工程割当:メイン/リリーフ/あふれ（≈1100行・最複雑）
│   │   └── exporter.py           # Excel/HTML出力（≈800行）
│   └── utils/
│       ├── normalizer.py         # テキスト正規化（全角→半角、エイリアス統一）
│       ├── csv_utils.py          # CSV読み書き（UTF-8/CP932自動判定）
│       └── excel_utils.py        # Excel操作（テーブル機能、インジェクション対策）
├── tests/
│   └── unit/
│       ├── test_sorter.py        # 仕分け・工程割当テスト（35テスト関数）
│       └── test_data_loader.py   # データローダーテスト
├── qt_setboard/                  # Qt/QML版セットボード（PySide6）
│   ├── main.py                   # Pythonバックエンド（Backendクラス）
│   ├── models.py                 # QMLモデル
│   ├── schedule.py               # スケジュール計算
│   ├── service.py                # サービス層
│   └── qml/                      # QMLフロントエンド
│       ├── Setboard.qml
│       └── components/
├── config/
│   └── ch_kanban_settings.json   # アプリケーション設定
├── docs/                         # 設計・仕様ドキュメント
│   ├── architecture.md           # システム構成・モジュール依存関係
│   ├── requirements.md           # 要件定義
│   ├── 仕分け・割り振りルール.md  # 運用ルール（実装の基準）
│   ├── 利用マニュアル.md          # ユーザー向け操作手順
│   ├── 起動・exe化ガイド.md       # bat起動・PyInstaller exe化手順 ★NEW
│   └── 参考: 既存ロジック系メモは仕分け・割り振りルールに統合済み
├── prompts/                      # AI開発支援プロンプト
│   ├── system.md                 # システムプロンプト（プロジェクト概要）
│   └── coding-rules.md           # コーディング規約
├── セットボード.html              # HTMLインタラクティブセットボード（≈1500行）
├── セットボード_デモ.html          # セットボードデモ版
├── 自動仕分け_JSON_セットボード.py  # セットボード連携版スクリプト
├── 自動仕分け_JSON_最新.py         # 最新の単体スクリプト版
├── 自動仕分け_人工追及.py          # 人工追及ロジック
├── 自動仕分けツール.spec           # PyInstaller設定（旧・単体スクリプト用）
├── CH_kanban_set_GUI.spec         # PyInstaller設定（GUI exe化用）★NEW
├── run_ch_kanban.bat              # ワンクリック起動バッチ ★NEW
├── 入車時間マスタ.xlsx             # 入車時間マスタデータ ★NEW
├── 自動仕分け設定.json             # 旧設定ファイル（base_dir）
├── 構成ドキュメント.md             # 仕分けツール全体仕様
├── 自動仕分け_人工追及_仕様書.md   # 詳細仕様書（全ロジック記載）
├── 引き継ぎメモ.md                 # この引き継ぎドキュメント
├── roadmap.md                     # 開発ロードマップ
├── .claude/context.md             # Claude用コンテキスト ★NEW
└── README.md                      # プロジェクト概要
```

---

## 4. モジュール依存関係

```
gui.py
├── data_loader.py  (CSV/Excel処理、正規化)
├── sorter.py       (グループ化・混載・山統合)
├── process_assigner.py  (メイン/リリーフ工程割当)
├── exporter.py     (Excel/HTML出力)
└── constants.py    (定数)
```

---

## 5. 処理パイプライン（全体フロー）

```
入力データ
  ├─ 出荷情報_全便_最新版.csv
  ├─ 出荷場一覧.csv
  ├─ 入車時間マスタ.xlsx（任意）
  ├─ 工程割当ルール.csv（オプション）
  └─ 自動仕分け設定.json / ch_kanban_settings.json

↓ [step1] data_loader.load_data()
  → CSV読込（UTF-8/CP932自動判定）→ 列検証 → 数値前処理
  → DataManager生成（便/受入/オーダー候補管理）

↓ [step2] sorter.run_pipeline()
  → filter_shipments()（便/受入/オーダー選択でフィルタ）
  → PLANKANBANSU展開（パレット枚数ぶん行を複製）
  → _add_arrival_time_column()（マスタ参照で入車時間付与）
  → assign_groups_sequential()（ファーストフィット・高さ上限2450mmでグルーピング）
  → _build_size1_mixed()（種類1の受入キーベース混載、最大3山）

↓ [step3] sorter.build_all_mountain_details()
  → 全サイズ種類の山を統合（種類1優先→種類4→その他）

↓ [step4] process_assigner.assign_processes_by_arrival_time()
  → メイン/リリーフ/あふれ3工程への自動割当
  → 前倒し最適化（主対象を遅らせない範囲で別山を先に処理）
  → 山数≤14: 全2^nパターン探索で最適解
  → 山数>14: ビーム探索（近似解）

↓ [step5] exporter.build_spo_export_df()
  → SPO用1山=1行DataFrame生成
  → 2山集荷ごとに照合180秒付与

↓ [step6] exporter.attach_pickup_start_time()
  → 入車時間マスタ参照→引取開始時間逆算

出力データ
  ├─ SPOアップロード用.xlsx
  ├─ SPOアップロード用_履歴.xlsx
  ├─ 工程別_山1レコード.xlsx
  ├─ かんばん詳細.xlsx
  ├─ 集約サマリ.xlsx
  ├─ セットボード.html
  └─ reassign_log.csv（手動変更操作ログ）
```

---

## 6. コアロジック詳細

### 6.1 仕分けロジック（sorter.py）

#### 6.1.1 入力正規化と前処理
- `run_pipeline()` の先頭で `data_manager.filter_shipments(selections)` を実行し、便名・受入・オーダー選択で対象を確定
- `PLANKANBANSU` を用いて1レコードをパレット単位へ展開（`np.repeat`）
- `master_df` がある場合は `_add_arrival_time_column()` で `納入先×NONYUHIBIN末尾2桁` から `入車時間` を付与
- `入車時間` が空の行は後段で「入車時間制約なし」として扱われる

#### 6.1.2 基本グループ化（全サイズ共通）
- 高さ制約は `DEFAULT_HEIGHT_CAP=2450mm`
- 山詰めは `assign_groups_sequential()` のファーストフィット（先頭から順に積み上げ、超えたら新山）
- サイズ種類ごとに処理し、並び順は以下:
  - 種類4: `移動工数` 降順 + `NONYUHIBIN` 昇順
  - その他: `移動工数` 降順 + `SYUKKASAKI` 昇順
- `入車時間` が存在する場合、同一サイズ種類内でも `入車時間` ごとに独立してグループ番号を再採番

#### 6.1.3 引取工数計算（山集計）
- 各グループの工数は以下で計算:
  - `引取工数 = Max移動工数 + BASE_ONE_TIME + (パレット数-1)*MIDDLE_WORK + パレット数*BASE_PER_PAL`
- `sorter.py` と `process_assigner.py` で同一式を使い、表示値とスケジューリング値を一致させている

#### 6.1.4 種類1混載（_build_size1_mixed）
- 対象は `サイズ種類=="1"` のみ
- `納入先` 欠損時は `納入先コード -> OData_納入先 -> SYUKKASAKI` の順で補完
- まず `NONYUHIBIN` 単位でローカル山を作成（高さ制約は同じ2450）
- ローカル山を `山ID` として集約後、2山/3山混載を実施

混載禁止条件:
- 同一 `納入先` かつ `NONYUHIBIN` が異なる組み合わせは同一混載山に入れない

混載構築手順:
1. 高さの大きい山から `g1` を選ぶ
2. `g1` に入る最大候補 `g2` を選ぶ
3. `g1+g2` の余白に入る `g3` を追加（あれば）
4. `id_map` で代表山へ寄せて `山通番` を連番化

#### 6.1.5 混載後の救済分割
- `_rescue_split_conflict_vendor()` で、混載山内の締切/開始下限衝突を検知して分割
- 判定ロジック:
  - 山内の `min_deadline`（各便の入車-10分最小）
  - 山内の `max_floor`（前便+10分最大）
  - `min_deadline < max_floor` なら衝突とみなし救済
- 救済方法:
  - 山内で最も締切が厳しい `納入先` を別山へ分離
  - `山通番` を新規採番して分割を確定

#### 6.1.6 全サイズ山統合（build_all_mountain_details）
- 統合順序:
  1. 種類1（混載後山通番を優先採用）
  2. 種類4（入車時間ありなら入車時間単位、なければ便単位で再積み）
  3. その他サイズ（グループ番号単位で連番）
- 最終明細は `山通番` 昇順、同一山内は `移動工数` 降順

### 6.2 割り振りロジック（process_assigner.py）

#### 6.2.1 時刻軸とシフト定義
- シフト開始秒:
  - 1直 `06:25` (`SHIFT_START_SECS[0]`)
  - 2直 `16:40` (`SHIFT_START_SECS[1]`)
- 締切バッファ:
  - `ARRIVAL_BUFFER_SECS=600`（入車10分前完了）
- 業務日タイムライン正規化:
  - `_to_operational_timeline_secs()` で `00:00-06:24` を `+24h` へ寄せる
  - 23時台〜翌1時台を単調増加時刻として扱うための処理

#### 6.2.2 マスタ参照マップの構築
- `master_map[(OData_納入先, NONYUHIBIN)] = 入車時間`
- `set_flag_map[(納入先,便)] = セットありフラグ(bool)`
- `vendor_shift_first_bin[(納入先,直)] = 各直最初の便番号`
- `vendor_sorted_groups[納入先] = 武部向けの入車時刻グループ`
- `vendor_bin_numbers[納入先] = 便番号一覧（日野循環便判定に利用）`

#### 6.2.3 山単位の締切・開始下限計算
- 山内行を走査し、以下を山単位に集約:
  - `締め切り_秒`: 山内最小の実効締切
  - `開始時間_秒`: 山内最大の開始下限（最も厳しい制約を採用）
  - `引取工数_秒`: 山単位再計算値

開始下限ロジック（優先順）:
1. 旧マスタ（セットありフラグ列なし）: 従来ルール
2. `set_flag=True`: 従来ルール（前便入車+10分）
3. `set_flag=False` かつ各直1便目: `直開始+15分`
4. `set_flag=False` かつ1便目以外: 前便入車+10分

従来ルール詳細:
- 武部: 同納入先の前時刻グループ+10分
- その他: 前便番号の入車+10分
- 日野系: 便番号を循環扱い（01の前便は最終便）

セットあり便の締切補正:
- 通常締切 `pickup-10分` に加えて、以下上限を適用
  - 1直上限 `15:20`
  - 2直上限 `01:35`（25:35）
- `effective_deadline = max(strict_deadline, limit_from_st, limit_from_pickup)`
  - `st` 基準と `pickup` 基準を併用して日付またぎ便の誤判定を抑止

#### 6.2.4 メイン工程の前倒し付き逐次割当
- `_pick_next_main_mountain()` で次山を選択
- 主対象は「締切最短の山」
- ただし「その時点で締切に間に合っていた山」を1つも新たに遅らせない範囲で、先に流せる候補山を前倒し採用（差分基準 = Tier 0-2 / Refs #122, #125）
- `_can_keep_primary_deadline()` で「候補先行 -> 主対象後続」の成立を判定するが、これは主対象1山しか見ていない（Refs #122 原因2）
- そのため主対象1山の判定に加えて、未割当の残り全山について「新たに締切を割る山が生じないか」を差分（after - before）で検証し、生じる場合はその候補を前倒し採用しない
- すでに間に合っていなかった山（詰み山 / Refs #96）は判定対象外とする。件数比較（>）ではなく差集合で判定する
- 締切内であれば余裕量を問わず採用する（Tier 2-5 / #125 で変更なしと確定）。余裕量による絞り込みは行わない（Refs #124 却下済み案2）

メイン可否判定:
- `actual_start = adjust(max(sequential_time, start_floor))`
- `work_end = calc_end_with_breaks(actual_start, work_duration)`
- `work_end <= deadline` ならメイン、超過ならリリーフへ振替

#### 6.2.5 リリーフ割当ルール
- リリーフ1山目:
  - `latest_start_to_meet_deadline(deadline, work)` と `start_floor` の両制約を満たす時刻
- リリーフ2山目以降:
  - `max(relief_end_time, start_floor)` から開始
- 照合180秒は後段で工程内順序に応じて付与（3山目/5山目/...）

#### 6.2.6 休憩処理
- `_adjust_start_for_breaks()`:
  - 開始時刻が休憩帯に入る場合、休憩後へ繰り下げ
  - 作業中に休憩へ突入する場合も繰り下げ
- `_calc_work_end_with_breaks()`:
  - 休憩跨ぎを考慮した終了時刻を逐次計算
- 昼休憩（10:40-11:25 / 20:55-21:40）は特別扱い:
  - 休憩前完了バッファ 10分
  - 再開オフセット 15分

#### 6.2.7 T0シフト（メインの表示時刻調整）
- 初回割当後、メイン山の `_raw_start/_raw_end` を使ってシフト量 `T0` を決定
- 候補:
  - `deadline - raw_end`（ただし `_is_anchored=False` 山のみ）
- `T0 = max(0, min(candidates))`
- 実適用は一律加算ではなく、以下制約を同時満足:
  - `t0_shifted`
  - `start_floor`
  - `seq_floor`（前山終了後）
- これにより、前詰め可能な山を不必要に遅らせない

#### 6.2.8 後段再スケジュール（安全弁）
- `_schedule_proc_rows()` で工程内時刻を再構築
- `prefer_deadline_order=True` の場合、締切優先順で並べ直し
- メインで締切超過が残る山はリリーフへ再振替
- 再振替後にメイン/リリーフ双方を再スケジュール

#### 6.2.9 リリーフ最小化
- 締切違反を増やさずに戻せる山をメインへ再昇格
- 状態評価指標:
  - `late_total`, `late_main`, `late_relief`, `relief_count`
- 「締切違反最小 + リリーフ最小」の辞書順で改善

#### 6.2.10 全探索 / ビーム探索
- 山数 `<=14`:
  - 全 `2^n` パターンを列挙（bit=0メイン、1リリーフ）
  - 各パターンを再スケジュールして最良状態を採用
- 山数 `>14`:
  - ビーム探索（`beam_width=6`, `max_rounds=4`）で近似
  - 近傍操作: 昇格、降格、スワップ

#### 6.2.11 あふれ判定
- 最終的にリリーフでも締切を満たせない山は `PROC_OVERFLOW` へ
- セットボード上は警告レーン扱い
- 出力には工程区分として保持し、手動再調整対象とする

### 6.3 既知課題（ロジック観点）

- 詳細は `docs/仕分け・割り振りルール.md` の「運用上の確認ポイント」を正とする
- 実装観点で特に注意する箇所:
  - セットあり便の1便目循環判定（直またぎ境界）
  - `00:00-06:24` の +24h 正規化依存
  - 深夜帯休憩が未定義の場合の締切誤判定
  - アンカー混在時のT0シフト歪み

### 6.4 データ読込（data_loader.py）

#### 正規化ルール
- 全角→半角変換
- エイリアス統一: 九州→KVC、TMK→KVC、日野E/H→日野EH
- 数字先頭ゼロ除去（受入コード）
- 時刻を"HH:MM"形式に統一

#### 候補抽出（3段階フィルタ）
1. 便名一覧（日野EH/武部は除外）
2. 便名→受入一覧
3. 便名+受入→オーダー一覧

#### 配車表インポート機能
- `parse_haisha_excel()`: 配車表Excel（Ｎ８ *.xlsm）から入車時間マスタを自動生成
- 1直/2直セクション別の解析
- ベンダー3行ブロック構造認識
- 便番号なしベンダー（武部等）の均等配分採番
- 納入先名マッピング: 日野プレス→日野、日野Eフード→日野EH、TMK→KVC、三栄SE→三栄

### 6.5 出力（exporter.py）

- **SPO用Excel**: 1山=1行、GroupedData JSON展開、照合180秒付与
- **かんばん明細Excel**: 工程別かんばん明細
- **セットボードHTML**: テーブル形式のタイムライン表示
- **履歴ファイル**: 既存ファイルへの追記
- **Excelインジェクション対策**: 先頭に `'` を付与

### 6.6 GUI（gui.py）

#### タブ構成（7タブ）
1. 基本集計（全サイズ種類グループ）
2. 混載(種類1)（mixed山サマリ）
3. 納入先別（mixed内訳）
4. 工程候補（ルール編集）
5. かんばん明細（工程別明細）
6. かんばん集計（工程別サマリ）
7. Total工数（全山工数一覧）

#### セットボード表示
- メイン/リリーフ/あふれの3レーン
- 開始時間順に配置
- 納入先/オーダー/引取工数を表示
- 山の手動移動（ドラッグ＆ドロップまたはダイアログ）

#### 画面仕様
- テーマ: CustomTkinter（ttkbootstrapは使わない）
- サイズ: 1400x900px
- フォント: Meiryo UI

### 6.3 データ読込（data_loader.py）

#### 正規化ルール
- 全角→半角変換
- エイリアス統一: 九州→KVC、TMK→KVC、日野E/H→日野EH
- 数字先頭ゼロ除去（受入コード）
- 時刻を"HH:MM"形式に統一

#### 候補抽出（3段階フィルタ）
1. 便名一覧（日野EH/武部は除外）
2. 便名→受入一覧
3. 便名+受入→オーダー一覧

#### 配車表インポート機能
- `parse_haisha_excel()`: 配車表Excel（Ｎ８ *.xlsm）から入車時間マスタを自動生成
- 1直/2直セクション別の解析
- ベンダー3行ブロック構造認識
- 便番号なしベンダー（武部等）の均等配分採番
- 納入先名マッピング: 日野プレス→日野、日野Eフード→日野EH、TMK→KVC、三栄SE→三栄

### 6.4 出力（exporter.py）

- **SPO用Excel**: 1山=1行、GroupedData JSON展開、照合180秒付与
- **かんばん明細Excel**: 工程別かんばん明細
- **セットボードHTML**: テーブル形式のタイムライン表示
- **履歴ファイル**: 既存ファイルへの追記
- **Excelインジェクション対策**: 先頭に `'` を付与

### 6.5 GUI（gui.py）

#### タブ構成（7タブ）
1. 基本集計（全サイズ種類グループ）
2. 混載(種類1)（mixed山サマリ）
3. 納入先別（mixed内訳）
4. 工程候補（ルール編集）
5. かんばん明細（工程別明細）
6. かんばん集計（工程別サマリ）
7. Total工数（全山工数一覧）

#### セットボード表示
- メイン/リリーフ/あふれの3レーン
- 開始時間順に配置
- 納入先/オーダー/引取工数を表示
- 山の手動移動（ドラッグ＆ドロップまたはダイアログ）

#### 画面仕様
- テーマ: CustomTkinter（ttkbootstrapは使わない）
- サイズ: 1400x900px
- フォント: Meiryo UI

---

## 7. 定数一覧（constants.py）

| 定数名 | 値 | 説明 |
|--------|-----|------|
| DEFAULT_HEIGHT_CAP | 2450 | 高さ上限(mm) |
| BASE_ONE_TIME | 187.64 | 基礎一回工数(秒) |
| MIDDLE_WORK | 3.247 | 中間作業工数(秒) |
| BASE_PER_PAL | 52 | パレット単位工数(秒) |
| PROC_MAIN | "メイン" | メイン工程ラベル |
| PROC_RELIEF | "リリーフ" | リリーフ工程ラベル |
| PROC_OVERFLOW | "あふれ" | あふれ工程ラベル |
| ARRIVAL_BUFFER_SECS | 600 | 入車時間の10分前締切(秒) |
| SHIFT_START_SECS | [06:25, 16:40] | 各直の開始時刻 |
| SET_FLAG_MAIN_LIMIT | [15:20, 01:35] | セットあり便のメイン上限 |

---

## 8. テスト構成

### tests/unit/test_sorter.py（35テスト関数、1000+行）
- `TestNormalizer`: 正規化テスト（3テスト）
- `TestGrouping`: グループ化テスト（6テスト）— ファーストフィット、高さ制約、混載条件
- `TestProcessAssigner`: 工程割当テスト（≈22テスト）— 時刻変換、休憩対応、メイン/リリーフ割当、前倒し最適化、全パターン探索、セットありフラグ、日野系循環便、日野01日付またぎ、リリーフ昇格
- `TestExporter`: 出力テスト（1テスト）— SPO引取工数と次山開始時間リンク

#### 最近追加されたテスト（重要）
- `test_overnight_hino_01_is_not_misclassified_as_shift_first_trip` — 日野01日付またぎ対応
- `test_set_flag_shift1_uses_main_limit_1520_to_reduce_relief` — セットあり1直ロジック
- `test_set_flag_shift2_uses_main_limit_0135_to_reduce_relief` — セットあり2直ロジック
- `test_relief_rows_can_be_promoted_back_to_main_when_feasible` — リリーフ→メイン昇格
- `test_dynamic_prefetch_*` — 前倒し最適化テスト群

### tests/unit/test_data_loader.py
- CH抽出テスト、列名エイリアス対応、ヘッダー自動検出、不要列無視、ベンダー名変換テスト

### テスト実行状況
- 現在のエラー: **なし**（全テストパス、コンパイルエラー/lintエラーもなし）
- 2026-06-02 切り分けメモ: `tests/unit/test_sorter.py` の `test_dynamic_prefetch_keeps_primary_deadline`（261行付近）と `test_assign_keeps_all_main_when_deadline_reorder_is_feasible`（364行付近）は、`process_assigner.py` の T0シフト修正（max(T0後, start_floor, seq_floor) 導入）の有無で失敗内容が変化せず、**既存課題**として扱う。

---

## 9. 開発ロードマップと進捗

### Phase 1 — MVP ✅完了
- [x] 仕分けロジック移植（グルーピング・混載）
- [x] 工程割当（メイン / リリーフ 2工程）
- [x] GUI（CustomTkinter 6タブ）
- [x] セットボード表示
- [x] ユニットテスト

### Phase 2 — 実運用（進行中）
- [ ] config/ch_kanban_settings.json からの設定読込
- [ ] SPO エクスポート動作検証
- [ ] 入車時間マスタ連携テスト
- [ ] エラーハンドリング強化

### Phase 3 — 改善
- [x] セットボードHTML出力
- [ ] 日報・ログ出力
- [ ] パフォーマンスチューニング（大量データ対応）

---

## 10. コーディング規約（必ず従うこと）

- **日本語コメント・変数名**を積極的に使用
- エンコーディングは **UTF-8** 統一
- GUIフォントは **Meiryo UI** 統一
- CustomTkinter `ctk.CTk*` 使用（ttkbootstrapは使わない）
- Treeview/ListboxはそのままtkinterのTkinterを使用
- pandas **vectorized操作を優先**（ループは最小限）
- 数値型変換は `errors="coerce"` で安全に
- Excel出力時は `_protect_excel_injection()` でインジェクション対策
- 全角→半角正規化を徹底
- ファイル読み込み時は存在チェック＋必須列検証
- GUIからの操作はtry/exceptでラップし、messagebox表示
- 起動時の `load_master()` は `self.after(200, ...)` で遅延実行

---

## 11. 設計上の重要な決定事項

1. **CH固有の1人体制**: メイン/リリーフ2工程に限定（複数工程対応は不要）
2. **入車時間ベース割当**: スケジューリングアルゴリズム（最適化問題として扱う）
3. **前倒し最適化**: 主対象の締切を守りながら、別山を先に処理してスループット向上
4. **全割当パターン探索**: 山数≤14で全最適解検索（NP困難の小規模問題に対応）
5. **セットありフラグ**: 旧マスタ互換（=1）と新セットなし便（=0）の両方に対応
6. **救済分割**: 混載山内の締切/開始下限競合を自動解消
7. **HTMLセットボード**: ブラウザベースで手動再割当を直感的に実装
8. **Qt/QML版セットボード**: PySide6ベースの別実装も並行開発中

---

## 12. 既存ドキュメント一覧（参照先）

| ファイル | 内容 | 重要度 |
|---------|------|--------|
| `docs/仕分け・割り振りルール.md` | 運用ルール（実装の基準） | ★★★ |
| `自動仕分け_人工追及_仕様書.md` | 全ロジック詳細仕様（60以上の関数仕様） | ★★★ |
| `docs/architecture.md` | システム構成・モジュール依存関係 | ★★ |
| `docs/requirements.md` | 要件定義 | ★★ |
| `構成ドキュメント.md` | 仕分けツール全体仕様 | ★★ |
| `docs/利用マニュアル.md` | ユーザー向け操作手順 | ★ |
| `docs/起動・exe化ガイド.md` | bat起動・PyInstaller exe化手順 | ★ |
| `prompts/system.md` | AIシステムプロンプト | ★★★ |
| `prompts/coding-rules.md` | コーディング規約 | ★★★ |
| `.claude/context.md` | Claude用プロジェクトコンテキスト | ★ |

---

## 13. 入力ファイル仕様

| ファイル名 | 形式 | 内容 |
|-----------|------|------|
| 出荷情報_CH_最新版.csv / 出荷情報_全便_最新版.csv | CSV | 全かんばんデータ |
| 出荷場一覧.csv | CSV | 出荷場マスタ |
| 入車時間マスタ.xlsx | Excel | 便名×納入先→入車時間 |
| 工程割当ルール.csv | CSV | 納入先→工程マッピング（オプション） |
| 配車表（Ｎ８ *.xlsm） | Excel | 配車表から入車時間を自動抽出 |
| 受入データExcel | Excel | 受入=CHのみ抽出してマスタ化 |

---

## 14. 現在のプロジェクト状態（2026-06-08時点）

### コードの状態
- **エラーなし**: コンパイルエラー、lintエラーともになし
- **テスト**: 全パス
- **GUI**: `python -m src.app.gui` で起動可能。`run_ch_kanban.bat` でワンクリック起動も可能。

### 直近の作業内容（05-30〜06-08）
- `process_assigner.py` の工程割当ロジック最適化
- `data_loader.py` が `出荷情報_CH_最新版.csv` を優先読み込みするよう更新
- `test_sorter.py` に山分け・混載系のテストを追加
- `run_ch_kanban.bat`（ワンクリック起動）と `CH_kanban_set_GUI.spec`（exe化用）を追加
- `docs/起動・exe化ガイド.md` を新規作成
- `docs/仕分け・割り振りルール.md` を運用ルールの基準に整理
- `.claude/context.md` を新規作成（Claude用コンテキスト）

### 既知バグ・リスク

現行の詳細は [docs/仕分け・割り振りルール.md](docs/仕分け・割り振りルール.md) に統合済み。

### 未解決事項
- **割り振りロジックの既知課題**（セクション6.3および `docs/仕分け・割り振りルール.md` 参照）
- Phase 2のタスクがすべて未完了（設定JSON読込、SPO検証、マスタ連携テスト、エラーハンドリング強化）
- Phase 3の日報・ログ出力、パフォーマンスチューニングが未着手

---

## 15. 新しいチャットで最初に伝えるべきこと

以下をそのまま貼り付けて使用できます:

---

```
## プロジェクト概要
CHかんばんセットプロジェクトの開発を継続してください。

### 前提情報
- ワークスペース: CHかんばんセット/
- 環境: conda環境 `DIG_new`、Python 3.x、CustomTkinter GUI
- 起動: `python -m src.app.gui`
- テスト: `python -m pytest tests/ -v`

### 重要ファイル（最初に読むべき）
1. `prompts/system.md` — プロジェクト概要とAI向け指示
2. `prompts/coding-rules.md` — コーディング規約
3. `docs/仕分け・割り振りルール.md` — 運用ルール（実装の基準）
4. `docs/architecture.md` — システム構成図
5. `docs/requirements.md` — 要件定義
6. `引き継ぎメモ.md` — この引き継ぎドキュメント

### 現在の状態
- Phase 1（MVP）は完了済み。全テストパス、エラーなし。
- 運用ルールの詳細は `docs/仕分け・割り振りルール.md` に集約済み。
- Phase 2（実運用）のタスクにも着手すべき段階。
- 最も複雑なモジュールは `src/services/process_assigner.py`（≈1100行）。

### 次にやるべきこと
1. まず `docs/仕分け・割り振りルール.md` と `docs/requirements.md` を確認
2. その後 roadmap.md の Phase 2 タスクに着手
```

---

## 16. 注意事項・落とし穴

1. **process_assigner.py は≈1100行で最も複雑**。変更時は必ずテストを実行すること。
2. **混載ロジック（sorter.py `_build_size1_mixed`）は≈350行**あり、条件分岐が多い。
3. **セットありフラグ=0（セットなし）の場合のルール**は後から追加されたもので、テストで挙動を確認済み。
4. **日野系の便循環**（01が最終便）は特殊処理がある。日付またぎ（23:xx→00:xx）の判定にも注意。
5. **配車表インポート**はベンダー3行ブロック構造を認識する特殊パーサーで、1直/2直セクション別に解析する。
6. **PowerShellにgitコマンドなし**。ファイルベースツールで対応すること。
7. **ルートの `.py` ファイル群**（`自動仕分け_JSON_最新.py` 等）は旧版の単体スクリプトで、`src/` のモジュール化版とは別物。
8. **セットあり便の各直1便目に巡回バグあり**（#1既知バグ）。修正前にテスト追加→修正→テスト確認の手順を踏むこと。
9. **exe化用のspecファイルが2つある**: `自動仕分けツール.spec`（旧・単体スクリプト用）と `CH_kanban_set_GUI.spec`（GUI用）。GUI用を使うこと。
10. **`run_ch_kanban.bat`** でVS Codeなしのワンクリック起動が可能。conda環境自動検出付き。
