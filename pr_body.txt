Part of #122 (原因2の一部: 前倒し採用が主対象の締切しか見ていない)

## 変更点
- `_makes_others_newly_late()` を新設（`_can_keep_primary_deadline()` の直後）
  差分基準（after - before）で「割り込まなければ間に合っていた山」が新たに落ちる場合のみ前倒しを見送る
- 既存の前倒し採用 if に `and not _makes_others_newly_late(...)` を1行追加
- docs/仕分け・割り振りルール.md §4.0 / §4.4 / §7 を同時更新（§7 同時更新義務）
- docs の「締切=入車-10分」表記（§2.4 / §4.1 / §4.3 の3箇所）を実装（`PICKUP_DEADLINE_BUFFER_SECS = 20 * 60`）
  に合わせて「入車-20分」に修正（#96 / #114 の誤記を訂正、#122で判明）
  ※ §4.2「前便入車+10分」（ARRIVAL_BUFFER_SECS）は対象外・変更なし
- 引き継ぎメモ.md を今回の作業内容で更新

## テスト
- 対象4ファイル: 8 passed（[GUARD] ログ 14 回発火。例: primary=2 candidate=5 newly_late=[3, 4, 9, 11]）
- 番犬6本: 80 passed, 3 skipped, 0 failed（3 skipped はベースライン）

## 既知の限界（レビュー時にご確認ください）
- EDF統合フィクスチャ（17山/20山）は EDF_COMPARE_MIN_YAMAS=15 超のため EDF比較経路に入り、
  before/after 差分ゼロ（悪化なし・改善なし）
- 実害データ（13山）は両閾値未満で本ガードが効く領域。実データのフィクスチャ化（#122 Phase 0 項2）は未実施のため、
  受入基準「超過 3件 → 0件」の検証は本PRの範囲外（フォローアップ）

## 不変条件
余裕量による事前除外なし(#124) / 差分基準(#96) / 床の非改変(#93) /
日野交錯評価順(#57) / EXHAUSTIVE_THRESHOLD=14 の行は不変

