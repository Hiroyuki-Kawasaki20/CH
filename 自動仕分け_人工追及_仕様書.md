# 自動仕分け_人工追及.py 仕様書

## 1. 概要

かんばん（パレット）の仕分け・工程割り振りを自動化する GUI アプリケーション。  
出荷情報 CSV と出荷場一覧 CSV をもとに、パレットを「山」にまとめ、入車時間に間に合うよう各工程へ自動配分し、SharePoint アップロード用 Excel を出力する。

| 項目 | 内容 |
|------|------|
| 言語 | Python 3 |
| GUI | tkinter + ttkbootstrap（cosmo テーマ） |
| 主要ライブラリ | pandas, numpy, openpyxl |
| 実行形態 | `.py` 直接実行 or PyInstaller exe |
| 出力形式 | Excel (.xlsx)、テーブル付き |

---

## 2. 入力データ

### 2.1 出荷情報\_全便\_最新版.csv

出荷対象のかんばん情報。1行＝1かんばん。

| 列名 | 型 | 説明 |
|------|----|------|
| SSYUKKA | str | 仕入先工区（出荷場一覧との照合キー） |
| SYUKKASAKI | str | 出荷先コード |
| SYUKKAKOKU | str | 出荷先工区（納入先工区） |
| UKEIRE | str | 受入コード |
| NONYUHIBIN | str | 納入日便番号（オーダー識別キー） |
| PLANKANBANSU | int | 計画かんばん枚数（パレット展開の複製数。1未満は1扱い） |
| 移動工数 | float | 1パレットの移動にかかる工数（秒） |
| 高さ | float | パレット高さ（mm） |
| サイズ種類 | str | パレットサイズ種類（"1"=標準混載可, "4"=武部専用, 他=混載不可） |
| 納入先 | str | 納入先名称 |
| 納入先コード | str | 納入先コード |
| HINBAN | str | 品番 |
| NONYUNO | str | 納入番号 |
| BANKAIHI | str | 番改日 |
| BANKAIBIN | str | 番改便 |
| SMAKER | str | メーカー |
| SKOKU | str | 国コード |
| SEBANGO | str | 背番号 |
| SAKUSEITIME | str | 作成タイム |
| 引取済 | str | 引取済フラグ |
| 登録日時 | str | 登録日時 |
| 登録者 | str | 登録者 |

### 2.2 出荷場一覧.csv

便名と受入の対応マスタ。出荷情報との照合に使用。

| 列名 | 型 | 説明 |
|------|----|------|
| 便名 | str | 便の名称（例: "KVC", "日野EH"）。表記ゆれは `_NAME_ALIASES` で正規化 |
| 受入 | str | 受入コード。数字のみの場合は先頭ゼロを除去して照合 |
| 仕入先工区 | str | 出荷情報の SSYUKKA と照合 |
| 納入先コード | str | 出荷情報の 納入先コード と照合 |
| 納入先工区 | str | 出荷情報の SYUKKAKOKU と照合 |

### 2.3 入車時間マスタ.xlsx

プログラムと同階層に配置。入車（トラック到着）時間を管理。

| 列名 | 型 | 説明 |
|------|----|------|
| OData_納入先 | str | 納入先名称 |
| NONYUHIBIN | str | 便番号（2桁ゼロ埋め） |
| 入車時間 | str | 入車時間 "HH:MM" 形式 |

### 2.4 工程割当ルール.csv

ユーザーの Documents フォルダに保存。納入先→工程の手動マッピング。

| 列名 | 型 | 説明 |
|------|----|------|
| 納入先 | str | 納入先名称 |
| 工程 | int | 割当工程番号（1/2/3） |

### 2.5 自動仕分け設定.json

設定ファイル。データフォルダパスを永続化。

```json
{
  "base_dir": "C:\\...\\データフォルダパス"
}
```

---

## 3. 定数・パラメータ

| 定数名 | 値 | 説明 |
|--------|----|------|
| DEFAULT_HEIGHT_CAP | 2450 | 1山あたりの高さ上限 (mm) |
| BASE_ONE_TIME | 187.64 | 引取工数の基本定数（秒）。1回の引取り作業で固定発生 |
| MIDDLE_WORK | 3.247 | 追加パレット1枚あたりの中間作業工数（秒） |
| BASE_PER_PAL | 52 | パレット1枚あたりの基本工数（秒） |
| DEFAULT_MIXING_KEY | "UKEIRE" | 種類1混載時のグルーピングキー列名 |

### 引取工数の計算式

$$
引取工数 = Max移動工数 + BASE\_ONE\_TIME + (パレット数 - 1) \times MIDDLE\_WORK + パレット数 \times BASE\_PER\_PAL
$$

---

## 4. データ正規化ルール

### 4.1 名称正規化 (`_NAME_ALIASES`)

| 入力値 | 正規化後 |
|--------|----------|
| 九州 | KVC |
| TMK | KVC |
| 日野E/H, 日野ｅ/ｈ, 日野e/h | 日野EH |

### 4.2 受入コード正規化

- 数字のみ → 先頭ゼロ除去（例: "01" → "1"）
- 文字列混在 → そのまま

### 4.3 時刻正規化

- 全角数字・コロン → 半角変換
- "HH:MM:SS" → "HH:MM"（秒切り捨て）
- ゼロ埋め2桁（例: "8:30" → "08:30"）

---

## 5. 処理パイプライン

### 5.1 データ読み込み (`load_data`)

```
設定JSON → base_dir取得 → CSV読込
  ├─ 出荷情報_全便_最新版.csv → df_shipments
  └─ 出荷場一覧.csv           → df_places
```

- 設定JSONにパスがなければフォルダ選択ダイアログを表示
- エンコーディングは UTF-8 → CP932 のフォールバック

### 5.2 候補抽出

GUI の左ペインで**便名 → 受入 → オーダー**の3段階フィルタを操作。

| 関数 | 入力 | 出力 |
|------|------|------|
| `get_routes()` | なし | 便名一覧 |
| `get_receipts_for_route(便名)` | 便名 | 受入一覧 |
| `get_orders_for_route_receipt(便名, 受入)` | 便名+受入 | NONYUHIBIN一覧 |
| `get_receipts_for_route_order(便名, オーダー)` | 便名+NONYUHIBIN | 受入一覧（逆引き） |

**照合ロジック** (`_mask_for_place_row`):
- `SSYUKKA == 仕入先工区`
- `納入先コード == 納入先コード`
- `SYUKKAKOKU == 納入先工区`
- `UKEIRE == 受入`（両方正規化して比較）

### 5.3 メインパイプライン (`run_pipeline`)

```
selections（便名×受入×オーダーのリスト）
    ↓
① 出荷情報のフィルタリング → filtered
    ↓
② PLANKANBANSU でパレット展開（複製）→ expanded
    ↓
③ サイズ種類ごとに基本グループ分け
    ├─ assign_groups_sequential(): 上から順に積んで高さ上限で切る
    ├─ 種類4: NONYUHIBIN昇順 → 移動工数降順でソート
    └─ 他: 移動工数降順 → SYUKKASAKI昇順でソート
    ↓
④ 種類1のみ混載処理
    ├─ 納入先ごとにローカルグループ分け
    ├─ 高さの大きい順に 2〜3 グループを混載マッチング
    └─ 混載条件: mixing_key（UKEIRE）が異なること
    ↓
⑤ 結果返却
```

### 5.4 全サイズ山統合 (`build_all_mountain_details`)

```
種類1混載山（山通番そのまま）
    ＋
種類4（武部専用）
    ├─ NONYUHIBIN単位で山分け（ストア無視）
    ├─ 同一NONYUHIBINでも高さ上限超なら次の山
    └─ NONYUHIBIN昇順で山通番を採番
    ＋
その他のサイズ種類
    └─ 既存グループ番号単位で山採番
```

### 5.5 工程割当

#### A. 納入先ベース工程付与 (`compute_proc_details`)
- 行ごとに納入先名 → `process_map`（工程割当ルール.csv）で工程(1/2/3)を付与
- マッチしない場合は工程 "4"

#### B. 山単位の工程決定（2方式）

| 方式 | 関数 | ロジック |
|------|------|---------|
| 優先度 | `compute_mountain_process(strategy="priority")` | 山内に工程1があれば1、なければ2、なければ3、なければ4 |
| 入車時間ベース | `assign_processes_by_arrival_time()` | 締切時間に間に合う最小番号の工程に割当（主方式） |

#### C. 入車時間ベース割り振りの詳細

```
1. 各山の締め切り時間（入車時間）と引取工数を算出
2. 締め切り時間の早い順にソート
3. 各山について1工程 → 2工程 → 3工程 → 4工程と順にチェック
4. その工程で作業を完了しても締め切りに間に合うなら割当
5. どの工程でも間に合わなければ4工程（最終工程）
```

**開始時間の算出:**
- 武部: 前グループの入車時間 + 10分
- 他の納入先: 前便（便番号 - 1）の入車時間 + 10分

**休憩時間（作業不可帯）:**

| 直 | 休憩 |
|----|------|
| 1直 | 08:30–08:40, 10:40–11:25, 12:55–13:05 |
| 2直 | 18:45–18:55, 20:55–21:40, 23:10–23:20 |

- 作業が休憩帯にまたがる場合 → 休憩終了 + 1分後に開始をずらす

---

## 6. 混載ポリシー (`SIZE_MIXING_POLICY`)

| サイズ種類 | 混載可否 | 最大混載グループ数 | 混載キー |
|------------|----------|--------------------| ---------|
| 1 | ○ | 3 | UKEIRE |
| 4 | × | - | - |
| default | × | - | - |

- 混載条件: 混載キー（UKEIRE）が**異なるグループ同士**を同じ山に積む
- 2山混載 → 余裕があれば3山混載まで試行
- マッチングは高さ合計の大きい順にグリーディに実施

---

## 7. 出力

### 7.1 SPOアップロード用.xlsx

SharePoint リストに取り込むための1山＝1行データ。

| 列名 | 説明 |
|------|------|
| タイトル | "山1", "山2"...（工程内での連番） |
| 工程 | "1工程" / "2工程" / "3工程" / "4工程" |
| groupdata | GroupedData と同値 |
| GroupedData | 山内パレットの JSON 配列（Power Apps 互換形式） |
| Max移動工数 | 山内の移動工数最大値（秒） |
| グループ番号 | 山通番 |
| パレット数 | 山内のパレット枚数 |
| 引取工数 | 計算式で算出（秒単位、整数） |
| 引取開始時間 | "HH:MM" 形式。入車時間マスタから算出 |
| id | 山通番 |
| 済 / 実施者 / 順番 / 照合日 / 照合済 / 割込み作業名 | 現場運用用の空列 |
| 更新日時 / 登録日時 | 出力時の ISO タイムスタンプ |

**GroupedData JSON の各要素:**

```json
{
  "OData__x30b9__x30c8__x30a2_": "ストア名",
  "NONYUHIBIN": "納入日便番号",
  "UKEIRE": "受入コード",
  "OData__x7d0d__x5165__x5148_": "納入先名",
  "SEBANGO": "背番号",
  "番号": 1,
  "引取済": ""
}
```

### 7.2 SPOアップロード用\_履歴.xlsx

実行のたびに追記される履歴ファイル。先頭に「出力日時」列を追加。

### 7.3 工程別かんばん\_山明細.xlsx

全パレットの詳細データ。GroupLabel 列（A/B/C...）付き。

---

## 8. GUI 構成

### 8.1 左ペイン（選択操作）

| 要素 | 機能 |
|------|------|
| 便名リスト | 複数選択可。選択で受入/オーダーを連動更新 |
| まとめチェック | ON: 便名→オーダー直接。OFF: 便名→受入→オーダーの3段階 |
| 受入リスト | まとめOFF時のみ操作可能 |
| オーダーリスト | ダブルクリックで追加可能 |
| 選択一覧ツリー | 便名×受入×オーダーの組み合わせ。Delete キーで削除 |
| 実行ボタン | Enter キーでも実行可能 |

### 8.2 右ペイン（結果タブ）

| タブ名 | 内容 |
|--------|------|
| 基本グループ | サイズ種類×グループ番号ごとの集計。混載は★+赤背景 |
| 種類1 混載 | 混載後の山一覧。total工数 = Max移動工数 + 引取工数 |
| 山別 納入先 | 山ごとの納入先数・一覧。2社以上は赤背景 |
| 山・工程一覧 | 山通番×工程の最終割当結果。工程別に色分け |
| Total工数 | 基本グループ＋種類1混載の全体工数一覧 |
| 入車時間マスタ | マスタの CRUD 操作（読込/保存/追加/削除/編集）+ 配車表からの自動インポート |

---

## 9. Excel出力のセキュリティ対策

- **数式インジェクション防止** (`_protect_excel_injection`): テキスト列の先頭が `=`, `+`, `-`, `@` の場合、先頭に `'` を付与
- **テーブル化** (`_add_table_exact`): `TableStyleMedium9` でExcelテーブルを作成。Power Automate での安全な処理を保証

---

## 10. 関数一覧

### 設定・IO

| 関数名 | 概要 |
|--------|------|
| `get_config_path()` | 設定JSONのパス取得（exe/py両対応） |
| `load_config()` / `save_config()` | 設定JSONの読み書き |
| `get_base_dir()` | データフォルダの取得（設定 or ダイアログ）  |
| `read_csv_ja()` / `write_csv_ja()` | 日本語CSV の読み書き（UTF-8/CP932 フォールバック） |
| `load_data()` | 出荷情報+出荷場一覧の読み込み |

### 正規化

| 関数名 | 概要 |
|--------|------|
| `_normalize_name()` | 名称のエイリアス解決 |
| `_normalize_dest_name()` | 納入先名の正規化 |
| `_normalize_route_name()` | 便名の正規化 |
| `_normalize_hhmm()` | 時刻の "HH:MM" 正規化 |
| `_normalize_ukeire()` | 受入コードの正規化（数字先頭ゼロ除去） |

### 候補抽出

| 関数名 | 概要 |
|--------|------|
| `_mask_for_place_row()` | 出荷場1行に対応する出荷情報のマスクを生成 |
| `get_routes()` | 便名一覧 |
| `get_receipts_for_route()` | 便名→受入一覧 |
| `get_orders_for_route_receipt()` | 便名+受入→オーダー一覧 |
| `get_orders_for_route()` | 便名→オーダー一覧（全受入横断） |
| `get_receipts_for_route_order()` | 便名+オーダー→受入一覧（逆引き） |

### パイプライン

| 関数名 | 概要 |
|--------|------|
| `run_pipeline()` | 選択条件から基本グループ＋種類1混載を一括実行 |
| `assign_groups_sequential()` | 上から順に高さ上限で山分け |
| `build_all_mountain_details()` | 全サイズの山を統合（種類1混載＋種類4＋他） |

### 工程割当

| 関数名 | 概要 |
|--------|------|
| `compute_proc_details()` | 納入先ベースで各行に工程付与 |
| `compute_mountain_process()` | 山単位の工程決定（優先度 or 多数決） |
| `assign_processes_by_arrival_time()` | 入車時間ベースで工程自動割り振り（メイン方式） |
| `compute_proc_summary()` | 工程サマリ（山×工程のクロス集計） |

### 集計・表示

| 関数名 | 概要 |
|--------|------|
| `compute_basic_groups()` | 基本グループの集計テーブル作成 |
| `compute_mixed_groups()` | 種類1混載の集計テーブル作成 |
| `compute_dest_by_mountain()` | 山別納入先の集計 |
| `compute_total_work_table()` | Total工数テーブル作成 |
| `build_simple_summary()` | 明細から簡易サマリを生成 |
| `add_group_label_by_koutei_yama()` | 工程×山通番で A/B/C... ラベルを付与 |

### Excel出力

| 関数名 | 概要 |
|--------|------|
| `export_kanban_xlsx()` | 工程別かんばん明細のXLSX出力 |
| `export_spo_xlsx()` | SPOアップロード用XLSX出力 |
| `append_to_spo_history()` | SPO履歴ファイルへの追記 |
| `export_per_process_mountains_xlsx()` | 工程別山1レコードXLSX出力 |

### 入車時間マスタ

| 関数名 | 概要 |
|--------|------|
| `get_master_path()` | マスタファイルパス取得 |
| `load_pickup_time_master_xlsx()` | マスタ読み込み（正規化込み） |
| `save_pickup_time_master_xlsx()` | マスタ保存 |
| `parse_haisha_excel()` | 配車表Excel（Ｎ８ *.xlsm）から入車時間マスタデータを自動抽出 |
| `attach_pickup_start_time()` | GroupedDataから引取開始時間を逆算して付与 |
| `adjust_pickup_time_for_same_bin()` | 同工程・同便の複数山の開始時間を連続調整 |

### 配車表インポート（`parse_haisha_excel`）

毎月作成される配車表Excel（Ｎ８ *.xlsm）から入車時間マスタを自動生成する。

**配車表の構造:**
- 1直（行1-36）/ 2直（行37-72）の2セクション
- 各ベンダー3行ブロック: 入車時間行 → ベンダー行（便番号付き） → 荷役時間行
- ベンダー行: C列=出荷場コード（例:01N）、E列=支給先名、各列に便番号
- 入車時間行: ベンダー行の1行上、同カラムにHH:MM形式の入車時間

**名称マッピング（`HAISHA_VENDOR_MAP`）:**

| 配車表の名称 | マスタの名称 |
|-------------|-------------|
| 日野プレス | 日野 |
| 日野Eフード | 日野EH |
| TMK | KVC |
| 三栄SE | 三栄 |
| 武部/高岡/元町/フタバ/刈谷 | そのまま |

**便番号省略ベンダー（武部等）の処理:**
- 配車表に便番号が記載されていないベンダーは、G列の配車数と入車時間数から均等配分で自動採番
- 2直→1直の順で連番を付与（例: 武部48便 = 2直26便 + 1直22便）
- インポート時に均等配分である旨を警告表示し、ユーザーが調整可能

### Power Apps/SPO互換

| 関数名 | 概要 |
|--------|------|
| `build_groupeddata_json_for_powerapps()` | Power Apps互換のJSON配列を生成 |
| `build_groupeddata_json_for_mountain()` | 山単位のGroupedData JSON生成 |
| `make_powerapps_like_collection()` | Power Apps ForAll/Collect相当のDF生成 |
| `build_spo_export_df()` | SPOリスト列に合わせた1山=1行DF構築 |
| `parse_groupeddata_json()` | Excel内のGroupedData JSON文字列をパース |

---

## 11. データフロー図

```
出荷情報CSV ─────────┐
                      ├─→ 照合(便名×受入×オーダー) ─→ filtered
出荷場一覧CSV ────────┘
                                                           │
                                                    PLANKANBANSU展開
                                                           │
                                                       expanded
                                                           │
                                              ┌───────────┼───────────┐
                                              ↓           ↓           ↓
                                           種類1        種類4       その他
                                         納入先別      NONYUHIBIN   グループ番号
                                         ローカル山    単位で山     単位で山
                                              │           │           │
                                         混載マッチ       │           │
                                         (2~3グループ)    │           │
                                              │           │           │
                                              └───────────┼───────────┘
                                                          │
                                                build_all_mountain_details
                                                          │
                                                compute_proc_details（納入先→工程）
                                                          │
                                                assign_processes_by_arrival_time
                                                   （入車時間ベース工程割振り）
                                                          │
                                                build_spo_export_df
                                                          │
                                                attach_pickup_start_time
                                                          │
                                                ┌─────────┴─────────┐
                                                ↓                   ↓
                                        SPOアップロード用.xlsx  SPO履歴.xlsx
```
